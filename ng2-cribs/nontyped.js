function multiplied(x, y) {
	return x * y;
}

// There is no way to tell if parameters are strings, integer etc
// Solution: TypeScript (created by Microsoft): allows to use TypeInformation in JavaScript
// We have to convert TypeScript to regular JavaScript to display in browser
multiplied(2, 3);